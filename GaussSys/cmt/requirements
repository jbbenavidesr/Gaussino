package GaussSys
version v49r2

branches cmt doc

#==================================================================
# Gauss packages
#==================================================================

# Temporarily to pick up locker mechanism for nighlty tests and builds
use GaudiPolicy         v*

# Temporarily to pick up fix in DDDB warning
use DDDB                v*   Det 

# Gauss application: the application is not built on Win32
use Gauss               v*   Sim

# LCG interfaces to generators, until available in LCGCMT
use LCG_Settings        v* 
use lhapdf              v*   LCG_GeneratorsInterfaces
use photospp            v*   LCG_GeneratorsInterfaces
use tauolapp            v*   LCG_GeneratorsInterfaces
use pythia6             v*   LCG_GeneratorsInterfaces
use pythia8             v*   LCG_GeneratorsInterfaces
use thepeg              v*   LCG_GeneratorsInterfaces
use herwigpp            v*   LCG_GeneratorsInterfaces
use rivet               v*   LCG_GeneratorsInterfaces
use alpgen              v*   LCG_GeneratorsInterfaces
use hijing              v*   LCG_GeneratorsInterfaces
use powhegbox           v*   LCG_GeneratorsInterfaces
use crmc                v*   LCG_GeneratorsInterfaces
use yoda                v*   LCG_GeneratorsInterfaces

# Generator packages: Basic configuration
use GenCuts             v*   Gen
use GENSER              v*   Gen
use EvtGen              v*   Gen
use Mint                v*   Gen
use EvtGenExtras        v*   Gen
use Generators          v*   Gen
use LbPythia8           v*   Gen

# Generators packages: alternative to Pythia8 for pp collisions
use LbPythia            v*   Gen
use LbHerwigpp          v*   Gen

# Generators packages: extensions to Pythia6 .or. Pythia8
use LbHard              v*   Gen
use LbAlpGen            v*   Gen 
use BcVegPy             v*   Gen
use GenXicc             v*   Gen
use LbBcVegPy           v*   Gen
use LbGenXicc           v*   Gen
use LbHidValley         v*   Gen
use LbOniaPairs         v*   Gen
use LbPowheg            v*   Gen

# Generators packages: alternative for p-A & A-A collisions
use LbHijing            v*   Gen
use LbCRMC              v*   Gen

# Generators packages: LHCb sampling results for Machine Induced Background
use LbMIB               v*   Gen

# Generators packages: LHCb particle gun generator
use LbPGuns             v*   Gen

# Generators packages: for CEP
use SuperChic           v*   Gen
use LbSuperChic         v*   Gen
use LPair               v*   Gen
use LbLPair             v*   Gen

# Generators packages: for prompt nuclei production, e.g. Deuteron
use LbBound             v*   Gen

# Simulation basic packages
use GiGa        	v*    Sim     
use GiGaCnv     	v*    Sim
use SimSvc              v*    Sim
use GiGaVisUI           v*    Sim
use VisSvc              v*    Vis 
use ClhepTools          v*    Tools

# Simulation specialized packages
use GaussKine           v*    Sim
use GaussAlgs		v*    Sim
use GaussTools          v*    Sim
use GaussRICH		v*    Sim
use GaussTracker	v*    Sim
use GaussCalo		v*    Sim
use GaussPhysics	v*    Sim
use GaussCherenkov      v*    Sim

# Monitoring packages
use GaussMonitor	v*    Sim
use CaloMoniSim         v*    Calo
use VeloGauss           v*    Velo
use MuonMoniSim         v*    Muon
use BcmMoniSim          v*    Bcm
use BlsMoniSim          v*    Bls
use VPMoniSim           v*    VP
use HCMoniSim           v*    HC

# Packages for MC generator tuning
use GenTune             v*    Gen  

# GMDL
use LbGDML              v*    Sim

# Visualization configuration
use XmlVis              v*    Vis

# Test packages for new releases or installations on new platforms
use GenTests            v*    Gen
use SimChecks           v*    Sim

# Allow to run the QMTests  
apply_pattern QMTest

# Declare this as a container package
apply_pattern container_package

# Allow the generation of QMTest summary 
apply_pattern QMTestSummarize

private
action TestProject "cmt br - cmt TestPackage ; cmt qmtest_summarize"
end_private

#====================================================================

